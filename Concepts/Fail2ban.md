#domain/security

# Fail2ban

A Linux intrusion-prevention tool: counts failed login attempts per host, and once a threshold is reached, handles the offending host as configured (typically a temporary ban via firewall rule). A common baseline hardening measure alongside disabling SSH password login and root login.

### Related
[[SSH]] [[Firewall]]

### Source:
[[25. Linux Security]]
