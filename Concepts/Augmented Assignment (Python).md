#domain/programming

# Augmented Assignment (Python)

Applies an operation to a variable and stores the result back in it, in one step (`+=`, `-=`, `*=`, `/=`, `//=`, `%=`, `**=`).

```python
count = 10
count += 5
print(count)  # 15
```

### Related
[[Integer (Python)]]  [[Float (Python)]]

### Source:
[[06. Numbers and mathematical operations]]
