#domain/networking

A markup language using custom tags to structure and describe data, similar in syntax to HTML but focused on carrying data rather than displaying it. One of several formats a web server's Content-Type header can indicate (alongside HTML, images, JSON, etc.).

### Related
[[HTML]] [[HTTP Headers]] [[HTTP]]

### Sources
[[21. HTTP in detail]]
