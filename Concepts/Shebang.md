#domain/os

# Shebang

`#!` followed by an interpreter path — the required first line of a script, tells the shell which interpreter should run it (e.g. `#!/bin/bash`).

### Related
[[bash]]

### Source:
[[11. Linux Shells]]
