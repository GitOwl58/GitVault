#domain/software

# SQL

A language used to ask questions (queries) of a database — a query doesn't change data, it displays requested information from a table.

Core keywords: `SELECT` (what to display) · `FROM` (where data comes from) · `WHERE` (filter condition) · `ORDER BY` (sort results)
### Related
[[Databases]]
### Source:
[[14. Database SQL Basics]]

